The names of the folders represent the type of controller it is. It can be a single controller or a hierarchical controller. 

Examples are:
- Position Controller - uses position jacobian
- Kinematic controller - uses kinematics
- Caretesian controller - in cartesian space

- MomentCartesian - combinantion with moment as dominant controller and cartesian as subordinate controller.